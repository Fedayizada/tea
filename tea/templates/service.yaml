apiVersion: v1
kind: Service
metadata:
  name: {{ include "tea.fullname" . }}
  labels:
    {{- include "tea.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetport }}
      protocol: TCP
      name: http
  selector:
    {{- include "tea.selectorLabels" . | nindent 4 }}
